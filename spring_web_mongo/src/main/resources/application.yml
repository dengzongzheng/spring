# Web容器配置
server:
  port: 8081
  undertow:
    accesslog:
      # 开启Undertow访问日志
      enabled: false


# 不使用发现服务，直接指定Service地址地表
# 不能与发现服务同时使用
#ribbon:
#  eureka:
#    enabled: false
#fs-jyhouse-second:
#  ribbon:
#    listOfServers: 10.10.33.24:8080

# Log 配置 级别大写 TRACE|DEBUG|INFO|WARN|ERROR
logging:
  level:
    #root: INFO
    com.dzz: INFO

spring:
  application:
    name: spring_web_mongo
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/